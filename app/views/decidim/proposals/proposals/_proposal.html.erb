<%= card_for proposal, from: proposal, link_whole_card: current_settings.votes_enabled? ? false : true %>

<% if current_settings.votes_enabled? %>
  <%= render "proposal_modal", proposal: proposal %>
<% end %>
